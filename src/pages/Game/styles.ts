import styled from 'styled-components'

export const GamePageContainer = styled.div`
  width: 100vw;
  height: 100vh;
  display: grid;
  place-items: center;
  position: relative;
  overflow: hidden;
`
